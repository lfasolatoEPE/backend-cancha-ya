{
  "/competicion/jugadores-ranking": {
    "get": {
      "summary": "Ranking de jugadores",
      "tags": ["Ranking"],
      "security": [{ "bearerAuth": [] }],
      "parameters": [
        { "name": "deporteId", "in": "query", "schema": { "type": "string", "format": "uuid" } }
      ],
      "responses": {
        "200": { "description": "Lista de jugadores ordenados por ranking" }
      }
    }
  },
  "/competicion/equipos-ranking": {
    "get": {
      "summary": "Ranking de equipos",
      "tags": ["Ranking"],
      "security": [{ "bearerAuth": [] }],
      "parameters": [
        { "name": "deporteId", "in": "query", "schema": { "type": "string", "format": "uuid" } }
      ],
      "responses": {
        "200": { "description": "Lista de equipos ordenados por ranking" }
      }
    }
  }
}
